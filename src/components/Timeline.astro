---
import type { ImageMetadata } from 'astro';
interface TimelineItemProps {
    title: string;
    subtitle: string;
    dateRange: string;
    description?: string;
    badges?: readonly string[];
    logo: ImageMetadata;
    stack?: readonly { name: string; image?: string }[];
}

interface Props {
    title: string;
    id: string;
    items: TimelineItemProps[];
}

const { title, id, items } = Astro.props;
---

<section id={id} class="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 gap-12 md:grid-cols-12">
        <div class="relative md:col-span-4">
            <h2 class="label-md sticky top-24 pt-2">{title}</h2>
        </div>

        <div class="space-y-16 md:col-span-8">
            {
                items.map((item) => (
                    <div class="group bg-surface-container-lowest rounded-3xl p-8 shadow-[0_10px_40px_rgba(45,51,56,0)] transition-colors duration-500 ease-out hover:shadow-[0_10px_40px_rgba(45,51,56,0.04)]">
                        <div class="mb-4 flex flex-col gap-4 md:flex-row md:items-start md:gap-8">
                            <div class="flex items-center gap-4 md:w-32 md:flex-col md:items-start md:gap-4">
                                <img
                                    src={item.logo.src}
                                    alt={`${item.title} logo`}
                                    class="ring-outline-variant/30 h-14 w-14 shrink-0 rounded-full bg-white object-cover shadow-sm ring-1 transition-transform duration-500"
                                    width={156}
                                    height={156}
                                />
                                <span class="label-sm text-on-surface-variant pt-1 whitespace-nowrap">
                                    {item.dateRange}
                                </span>
                            </div>
                            <div class="flex-1">
                                <h3 class="headline-lg mb-1 text-2xl md:text-3xl">
                                    {item.title}
                                </h3>
                                <div class="body-lg text-primary">
                                    {item.subtitle}
                                </div>
                            </div>
                        </div>

                        {(item.description ||
                            (item.badges && item.badges.length > 0) ||
                            (item.stack && item.stack.length > 0)) && (
                            <div class="md:pl-40">
                                {item.description && (
                                    <p class="body-lg text-on-surface-variant mt-4 leading-relaxed">
                                        {item.description}
                                    </p>
                                )}
                                {item.badges && item.badges.length > 0 && (
                                    <div class="mt-6 flex flex-wrap gap-2">
                                        {item.badges.map((badge: string) => (
                                            <span class="bg-surface-container-highest text-on-surface-variant rounded-full px-3 py-1 text-xs font-medium tracking-wider uppercase">
                                                {badge}
                                            </span>
                                        ))}
                                    </div>
                                )}
                                {item.stack && item.stack.length > 0 && (
                                    <div class="mt-6 flex flex-wrap gap-3">
                                        {item.stack.map((tech) => (
                                            <div class="bg-surface-container text-on-surface border-outline-variant/30 hover:bg-surface-container-high hover:border-outline-variant/60 flex items-center gap-2 rounded-lg border px-3 py-1.5 text-sm font-medium shadow-xs transition-colors">
                                                {tech.image && (
                                                    <img
                                                        src={tech.image}
                                                        alt={tech.name}
                                                        class="h-4 w-4 object-contain opacity-80"
                                                        loading="lazy"
                                                    />
                                                )}
                                                <span>{tech.name}</span>
                                            </div>
                                        ))}
                                    </div>
                                )}
                            </div>
                        )}
                    </div>
                ))
            }
        </div>
    </div>
</section>
